# L3 三指标独立榜浮出 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 写一个只读后处理脚本，把现有全量评分 CSV 里的 L3 三指标组合滤过拟合后浮出为独立榜，并给每条标注"第三个条件的增量价值"。

**Architecture:** 单文件 `data/profit_mining/surface_l3.py`，纯 pandas、宿主直接跑、不进容器、不依赖 features.py。函数链：load_scores → (build_pair_lift + l3_marginal 子集集合匹配算增量) → surface(护栏+排序+TopN) → write_outputs(CSV+md)。不动 mine_combos_v2.py / 主榜 / 不重挖掘。

**Tech Stack:** Python3, pandas。测试为无 pytest 脚本：`python3 test_surface_l3.py`，`assert`+`print("OK ...")`，结尾 `print("ALL OK")`。

**Spec:** `docs/superpowers/specs/2026-06-12-l3-surface-design.md`

**数据源:** `/home/tdxback/report/各组方案_全部评分_20260601_204028.csv`（81152 行，含 5195 条 L3）。列：`分组,方案,支持数,盈利覆盖率,非盈利覆盖率,提升度,条件内胜率,基线胜率,胜率增益,层级`。`方案` 用 `" + "` 连接条件名；提升度 `999` = inf 哨兵（cl=0 过拟合）。

**约定:** 所有代码在 `data/profit_mining/`。测试在该目录 `python3 test_surface_l3.py` 运行。

---

### Task 1: 加载与方案切分 load_scores / split_conditions

**Files:**
- Create: `data/profit_mining/surface_l3.py`
- Test: `data/profit_mining/test_surface_l3.py`

- [ ] **Step 1: Write the failing test** — create `test_surface_l3.py`:

```python
# test_surface_l3.py —— L3独立榜浮出 合成数据测试（python3 test_surface_l3.py）
import pandas as pd
import surface_l3 as S


def test_split_conditions():
    assert S.split_conditions("a + b + c") == ["a", "b", "c"]
    # 含 >= / <= 的真实条件名不被误切
    assert S.split_conditions("相对强弱>=5 + 突破前高") == ["相对强弱>=5", "突破前高"]
    assert S.split_conditions("量比>=1.3") == ["量比>=1.3"]
    print("OK split_conditions")


def test_load_scores(tmp_path="/tmp/l3_scores.csv"):
    pd.DataFrame([
        {"分组": "G", "方案": "a + b", "支持数": "200", "盈利覆盖率": "0.5",
         "非盈利覆盖率": "0.1", "提升度": "2.0", "条件内胜率": "0.6",
         "基线胜率": "0.4", "胜率增益": "0.2", "层级": "L2"},
    ]).to_csv(tmp_path, index=False, encoding="utf-8-sig")
    df = S.load_scores(tmp_path)
    assert df["支持数"].dtype.kind in "if"        # 数值化
    assert abs(df["提升度"].iloc[0] - 2.0) < 1e-9
    assert df["方案"].iloc[0] == "a + b"           # 字符串列保持
    print("OK load_scores")


if __name__ == "__main__":
    test_split_conditions()
    test_load_scores()
    print("ALL OK")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 test_surface_l3.py`
Expected: FAIL — `ModuleNotFoundError: No module named 'surface_l3'`

- [ ] **Step 3: Write minimal implementation** — create `surface_l3.py`:

```python
# surface_l3.py —— L3 三指标独立榜浮出：后处理 各组方案_全部评分_*.csv，
# 筛 L3 + 算"第三条件增量价值" + 护栏 + 排序 → 独立榜 CSV + md。纯 pandas，只读。
import os, sys, glob, datetime
import pandas as pd

NUM_COLS = ["支持数", "盈利覆盖率", "非盈利覆盖率", "提升度",
            "条件内胜率", "基线胜率", "胜率增益"]


def split_conditions(plan_str):
    """方案名按 ' + ' 切成条件名列表。条件名内含 >=/<= 不受影响。"""
    return [p for p in str(plan_str).split(" + ")]


def load_scores(path):
    """读全量评分 CSV，数值列转 float。字符串列(分组/方案/层级)保持。"""
    df = pd.read_csv(path, encoding="utf-8-sig", low_memory=False)
    for c in NUM_COLS:
        if c in df.columns:
            df[c] = pd.to_numeric(df[c], errors="coerce")
    return df
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 test_surface_l3.py`
Expected: `ALL OK`

- [ ] **Step 5: Commit**

```bash
cd /home/tdxback/aiagents-stock
git add data/profit_mining/surface_l3.py data/profit_mining/test_surface_l3.py
git commit -m "feat: L3榜 load_scores + split_conditions"
```

---

### Task 2: 子集集合匹配 + 增量价值 build_pair_lift / l3_marginal

**Files:**
- Modify: `data/profit_mining/surface_l3.py`
- Test: `data/profit_mining/test_surface_l3.py`

- [ ] **Step 1: APPEND this test** and add the calls to `__main__` (before `print("ALL OK")`):

```python
def test_build_pair_lift_and_marginal():
    # L2 子集表(故意乱序: a/b 存成 "b + a")
    l2 = pd.DataFrame([
        {"方案": "b + a", "提升度": 2.0, "层级": "L2"},
        {"方案": "a + c", "提升度": 3.0, "层级": "L2"},
        {"方案": "c + b", "提升度": 1.5, "层级": "L2"},
    ])
    pl = S.build_pair_lift(l2)
    assert pl[frozenset(("a", "b"))] == 2.0      # 集合匹配,无视顺序
    assert pl[frozenset(("b", "c"))] == 1.5
    # L3 "a + b + c" 提升度3.5 → 子集 {a,b}=2,{a,c}=3,{b,c}=1.5 → 最优3.0(a+c) → 增量0.5
    subset, blift, inc = S.l3_marginal("a + b + c", 3.5, pl)
    assert blift == 3.0, blift
    assert abs(inc - 0.5) < 1e-9, inc
    assert frozenset(S.split_conditions(subset)) == frozenset(("a", "c")), subset
    print("OK build_pair_lift_and_marginal")


def test_marginal_inf_sentinel():
    pl = {frozenset(("a", "b")): 2.0, frozenset(("a", "c")): 3.0,
          frozenset(("b", "c")): 999.0}
    # L3 提升度999(inf) → 增量 inf
    _, _, inc1 = S.l3_marginal("a + b + c", 999.0, pl)
    assert inc1 == float("inf"), inc1
    # L3 有限 但最优子集是999 → 增量 -inf
    pl2 = {frozenset(("a", "b")): 999.0, frozenset(("a", "c")): 3.0,
           frozenset(("b", "c")): 1.5}
    _, blift2, inc2 = S.l3_marginal("a + b + c", 4.0, pl2)
    assert blift2 == 999.0 and inc2 == float("-inf"), (blift2, inc2)
    print("OK marginal_inf_sentinel")
```

`__main__` 变为：
```python
if __name__ == "__main__":
    test_split_conditions()
    test_load_scores()
    test_build_pair_lift_and_marginal()
    test_marginal_inf_sentinel()
    print("ALL OK")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 test_surface_l3.py`
Expected: FAIL — `AttributeError: module 'surface_l3' has no attribute 'build_pair_lift'`

- [ ] **Step 3: APPEND implementation to `surface_l3.py`:**

```python
def build_pair_lift(l2_df):
    """单组 L2 行 → {frozenset({条件x,条件y}): 提升度}。用集合做键,无视存储顺序。"""
    d = {}
    for _, r in l2_df.iterrows():
        names = split_conditions(r["方案"])
        if len(names) == 2:
            d[frozenset(names)] = r["提升度"]
    return d


def l3_marginal(plan_str, l3_lift, pair_lift):
    """L3 三元组 → (最优两两子集 str, 子集提升度, 增量提升度)。
    增量 = L3提升度 − 三个两两子集中提升度最高者。提升度 999 视为 inf 哨兵。"""
    names = split_conditions(plan_str)
    pairs = [(names[0], names[1]), (names[0], names[2]), (names[1], names[2])]
    best_pair, best_lift = None, float("-inf")
    for x, y in pairs:
        lift = pair_lift.get(frozenset((x, y)))
        if lift is None:
            continue
        if lift > best_lift:
            best_lift, best_pair = lift, (x, y)
    if best_pair is None:
        return ("", float("nan"), float("nan"))
    subset_str = f"{best_pair[0]} + {best_pair[1]}"
    if l3_lift >= 999:
        inc = float("inf")
    elif best_lift >= 999:
        inc = float("-inf")
    else:
        inc = l3_lift - best_lift
    return (subset_str, best_lift, inc)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 test_surface_l3.py`
Expected: `ALL OK`

- [ ] **Step 5: Commit**

```bash
cd /home/tdxback/aiagents-stock
git add data/profit_mining/surface_l3.py data/profit_mining/test_surface_l3.py
git commit -m "feat: L3榜 子集集合匹配 build_pair_lift + 增量价值 l3_marginal"
```

---

### Task 3: 护栏+排序+组装 surface

**Files:**
- Modify: `data/profit_mining/surface_l3.py`
- Test: `data/profit_mining/test_surface_l3.py`

- [ ] **Step 1: APPEND this test** and add `test_surface()` to `__main__`:

```python
def _scores_df():
    base = {"条件内胜率": 0.6, "基线胜率": 0.4, "胜率增益": 0.2, "非盈利覆盖率": 0.1}
    rows = [
        # G 组 L2 子集
        {"分组": "G", "方案": "a + b", "支持数": 200, "盈利覆盖率": 0.5, "提升度": 2.0, "层级": "L2", **base},
        {"分组": "G", "方案": "a + c", "支持数": 200, "盈利覆盖率": 0.5, "提升度": 3.0, "层级": "L2", **base},
        {"分组": "G", "方案": "b + c", "支持数": 200, "盈利覆盖率": 0.5, "提升度": 1.5, "层级": "L2", **base},
        # 达标 L3
        {"分组": "G", "方案": "a + b + c", "支持数": 150, "盈利覆盖率": 0.30, "提升度": 3.5, "层级": "L3", **base},
        # 支持不足(99<100) 应被剔
        {"分组": "G", "方案": "a + b + d", "支持数": 99, "盈利覆盖率": 0.30, "提升度": 9.0, "层级": "L3", **base},
        # 覆盖不足(0.19<0.20) 应被剔
        {"分组": "G", "方案": "a + c + e", "支持数": 200, "盈利覆盖率": 0.19, "提升度": 9.0, "层级": "L3", **base},
        # 边界: 支持=100 覆盖=0.20 应保留
        {"分组": "G", "方案": "b + c + f", "支持数": 100, "盈利覆盖率": 0.20, "提升度": 1.0, "层级": "L3", **base},
    ]
    return pd.DataFrame(rows)


def test_surface():
    board = S.surface(_scores_df(), support_min=100, cover_min=0.20, topn=15)
    # 仅 a+b+c 与 b+c+f 达标; 按提升度降序 → a+b+c(3.5) 在前
    assert list(board["方案"]) == ["a + b + c", "b + c + f"], list(board["方案"])
    r0 = board.iloc[0]
    assert abs(r0["增量提升度"] - 0.5) < 1e-9, r0["增量提升度"]   # 3.5 - max(2,3,1.5)=3.0
    assert frozenset(S.split_conditions(r0["最优两两子集"])) == frozenset(("a", "c"))
    # 输出列齐全且顺序正确
    assert list(board.columns) == ["分组", "方案", "支持数", "盈利覆盖率", "条件内胜率",
                                   "基线胜率", "胜率增益", "提升度", "最优两两子集",
                                   "子集提升度", "增量提升度"], list(board.columns)
    print("OK surface")


def test_surface_topn():
    # 同组 20 条达标 L3 → Top15 截断
    base = {"条件内胜率": 0.6, "基线胜率": 0.4, "胜率增益": 0.2, "非盈利覆盖率": 0.1}
    rows = [{"分组": "H", "方案": "x + y", "支持数": 300, "盈利覆盖率": 0.5,
             "提升度": 5.0, "层级": "L2", **base}]
    for i in range(20):
        rows.append({"分组": "H", "方案": f"x + y + z{i}", "支持数": 200,
                     "盈利覆盖率": 0.30, "提升度": float(i), "层级": "L3", **base})
    board = S.surface(pd.DataFrame(rows), support_min=100, cover_min=0.20, topn=15)
    assert len(board) == 15, len(board)
    assert board["提升度"].iloc[0] == 19.0      # 降序,最高在前
    print("OK surface_topn")
```

`__main__` 追加 `test_surface()` 和 `test_surface_topn()`。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 test_surface_l3.py`
Expected: FAIL — `AttributeError: module 'surface_l3' has no attribute 'surface'`

- [ ] **Step 3: APPEND implementation to `surface_l3.py`:**

```python
OUT_COLS = ["分组", "方案", "支持数", "盈利覆盖率", "条件内胜率", "基线胜率",
            "胜率增益", "提升度", "最优两两子集", "子集提升度", "增量提升度"]


def surface(df, support_min=100, cover_min=0.20, topn=15):
    """筛 L3 + 算增量 + 护栏(支持≥support_min 且 覆盖≥cover_min) + 每组按提升度Top N。
    返回榜单 DataFrame(列见 OUT_COLS)。"""
    out = []
    for grp, g in df.groupby("分组", sort=False):
        pair_lift = build_pair_lift(g[g["层级"] == "L2"])
        l3 = g[(g["层级"] == "L3") &
               (g["支持数"] >= support_min) &
               (g["盈利覆盖率"] >= cover_min)].copy()
        if l3.empty:
            continue
        marg = l3["方案"].map(lambda p: l3_marginal(
            p, l3.loc[l3["方案"] == p, "提升度"].iloc[0], pair_lift))
        l3["最优两两子集"] = [m[0] for m in marg]
        l3["子集提升度"] = [round(m[1], 3) if pd.notna(m[1]) else m[1] for m in marg]
        l3["增量提升度"] = [round(m[2], 3) if pd.notna(m[2]) else m[2] for m in marg]
        l3 = l3.sort_values("提升度", ascending=False).head(topn)
        out.append(l3[OUT_COLS])
    if not out:
        return pd.DataFrame(columns=OUT_COLS)
    return pd.concat(out, ignore_index=True)
```

> 注：`l3_marginal` 的第二参数用 `l3.loc[l3["方案"]==p,"提升度"].iloc[0]` 取该方案行的提升度。若同组存在同名方案（实际不会，方案在组内唯一），取首条即可。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 test_surface_l3.py`
Expected: `ALL OK`

- [ ] **Step 5: Commit**

```bash
cd /home/tdxback/aiagents-stock
git add data/profit_mining/surface_l3.py data/profit_mining/test_surface_l3.py
git commit -m "feat: L3榜 surface 护栏+增量列+每组提升度Top15"
```

---

### Task 4: 输出 CSV + md write_outputs

**Files:**
- Modify: `data/profit_mining/surface_l3.py`
- Test: `data/profit_mining/test_surface_l3.py`

- [ ] **Step 1: APPEND this test** and add `test_write_outputs()` to `__main__`:

```python
def test_write_outputs(tmpdir="/tmp/l3_out"):
    import os, glob, shutil
    shutil.rmtree(tmpdir, ignore_errors=True)
    os.makedirs(tmpdir, exist_ok=True)
    board = S.surface(_scores_df(), support_min=100, cover_min=0.20, topn=15)
    paths = S.write_outputs(board, tmpdir, "20260612_000000")
    csvs = glob.glob(os.path.join(tmpdir, "L3独立榜_2*.csv"))
    mds = glob.glob(os.path.join(tmpdir, "L3独立榜_对比_*.md"))
    assert len(csvs) == 1 and len(mds) == 1, (csvs, mds)
    head = open(csvs[0], encoding="utf-8-sig").readline()
    assert "增量提升度" in head and "最优两两子集" in head, head
    body = open(mds[0], encoding="utf-8").read()
    assert "真协同" in body or "凑数" in body         # 判定标注存在
    print("OK write_outputs")
```

`__main__` 追加 `test_write_outputs()`。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 test_surface_l3.py`
Expected: FAIL — `AttributeError: module 'surface_l3' has no attribute 'write_outputs'`

- [ ] **Step 3: APPEND implementation to `surface_l3.py`:**

```python
def write_outputs(board, out_dir, ts):
    """榜单 → CSV(全量) + md(每组Top5,标注正增量真协同/负零增量凑数)。返回 [csv, md] 路径。"""
    os.makedirs(out_dir, exist_ok=True)
    csv_path = os.path.join(out_dir, f"L3独立榜_{ts}.csv")
    board.to_csv(csv_path, index=False, encoding="utf-8-sig")
    md_path = os.path.join(out_dir, f"L3独立榜_对比_{ts}.md")
    L = ["# L3 三指标独立榜 — 增量价值对比", "", f"生成 {ts}",
         "护栏: 支持≥100 且 盈利覆盖率≥0.20，每组按提升度 Top15；"
         "增量提升度 = L3提升度 − 最优两两子集提升度（正=第三条真协同，负/零=凑数）", ""]
    for grp, sub in board.groupby("分组", sort=False):
        L.append(f"## {grp}（{len(sub)} 条）")
        L.append("| 方案 | 提升度 | 覆盖率 | 增量 | 最优子集 | 判定 |")
        L.append("|---|---|---|---|---|---|")
        for _, r in sub.head(5).iterrows():
            inc = r["增量提升度"]
            good = isinstance(inc, (int, float)) and inc == inc and inc > 0
            verdict = "✅真协同" if good else "⚠️凑数"
            L.append(f"| {r['方案']} | {r['提升度']} | {r['盈利覆盖率']:.0%} | "
                     f"{inc} | {r['最优两两子集']} | {verdict} |")
        L.append("")
    open(md_path, "w", encoding="utf-8").write("\n".join(L))
    return [csv_path, md_path]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 test_surface_l3.py`
Expected: `ALL OK`

- [ ] **Step 5: Commit**

```bash
cd /home/tdxback/aiagents-stock
git add data/profit_mining/surface_l3.py data/profit_mining/test_surface_l3.py
git commit -m "feat: L3榜 write_outputs CSV+md(真协同/凑数判定)"
```

---

### Task 5: main 驱动 + 真实数据跑

**Files:**
- Modify: `data/profit_mining/surface_l3.py`

- [ ] **Step 1: APPEND `main()` to `surface_l3.py`:**

```python
def main():
    arg = sys.argv[1] if len(sys.argv) > 1 else None
    if arg:
        path = arg
    else:
        cands = sorted(glob.glob("/home/tdxback/report/各组方案_全部评分_*.csv"))
        if not cands:
            print("找不到 各组方案_全部评分_*.csv，请先跑 mine_combos_v2.py"); sys.exit(1)
        path = cands[-1]
    df = load_scores(path)
    if int((df["层级"] == "L3").sum()) == 0:
        print(f"{os.path.basename(path)} 无 L3 行"); sys.exit(1)
    board = surface(df)
    ts = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
    paths = write_outputs(board, "/home/tdxback/report", ts)
    print(f"[L3榜] {len(board)} 行（源 {os.path.basename(path)}）")
    for p in paths:
        print("  写", p)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 语法自检 + 单测仍绿**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 -c "import ast; ast.parse(open('surface_l3.py').read()); print('SYNTAX OK')" && python3 test_surface_l3.py`
Expected: `SYNTAX OK` 然后 `ALL OK`

- [ ] **Step 3: 真实数据跑**

Run: `cd /home/tdxback/aiagents-stock/data/profit_mining && python3 surface_l3.py`
Expected: 打印 `[L3榜] N 行（源 各组方案_全部评分_20260601_204028.csv）` 并列出写入的 CSV + md 路径。

- [ ] **Step 4: 核验产物**

Run:
```bash
ls -lt /home/tdxback/report/L3独立榜_*.csv /home/tdxback/report/L3独立榜_对比_*.md | head
head -3 /home/tdxback/report/L3独立榜_*.csv | head -5
```
Expected: CSV 列头含 `最优两两子集,子集提升度,增量提升度`；至少若干组有达标 L3（若全空说明护栏过严，回报用户决定放宽阈值）。人工读 md 看每组 Top5 的真协同/凑数标注是否合理（尤其样例 `相对强弱>=5 + 相对强弱大于5 + 突破前高` 应是低/负增量=凑数）。

- [ ] **Step 5: Commit**

```bash
cd /home/tdxback/aiagents-stock
git add data/profit_mining/surface_l3.py
git commit -m "feat: L3榜 main 驱动(默认取最新评分CSV,写/home/tdxback/report)"
```

---

## Self-Review

**Spec 覆盖核对：**
- §2 单文件 surface_l3.py 纯 pandas 只读 → Task1-5 ✓
- §3 数据流(分组/L2子集表/L3解析/增量/护栏/TopN) → Task2 build_pair_lift+l3_marginal、Task3 surface ✓
- §4.1 子集集合匹配(frozenset 防乱序) → Task2 build_pair_lift + test 乱序断言 ✓
- §4.3 inf 哨兵(999) → Task2 l3_marginal + test_marginal_inf_sentinel ✓
- §5 组件 load_scores/build_pair_lift/split_conditions/l3_marginal/surface/write_outputs/main → Task1-5 全覆盖 ✓
- §6 输出列与文件名(L3独立榜_/对比_md) → Task3 OUT_COLS、Task4 write_outputs ✓
- §7 错误处理(CSV不存在/无L3/某组无达标) → Task5 main(无L3退出)、Task3 surface(组空跳过)、Task4 ✓
- §8 测试(集合匹配/增量/护栏边界/TopN/inf) → Task1-4 各测试 ✓
- §9 不改 mine_combos_v2/主榜/不重挖掘 → 全为新文件,零改动既有 ✓

**占位符扫描：** 无 TBD/TODO；每步含完整代码。✓

**类型/签名一致性：**
- `l3_marginal(plan_str, l3_lift, pair_lift)` → 3 元组 `(subset_str, best_lift, inc)`，Task2 定义、Task3 surface 调用一致 ✓
- `build_pair_lift(l2_df)` → dict[frozenset→lift]，Task2 定义、Task3 调用一致 ✓
- `surface(df, support_min=100, cover_min=0.20, topn=15)` → DataFrame[OUT_COLS]，Task3 定义、Task4/Task5 调用一致 ✓
- `write_outputs(board, out_dir, ts)` → [csv, md]，Task4 定义、Task5 调用一致 ✓
- `OUT_COLS` 在 Task3 定义并被 test_surface 断言列顺序一致 ✓
```
