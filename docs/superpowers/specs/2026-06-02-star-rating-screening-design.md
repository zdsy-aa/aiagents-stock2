# 核心层 / 精选层 5★→1★ 上涨概率分级 —— 设计文档

- 日期：2026-06-02
- 分支：feat/liumai-and-combo-screening
- 关联：[[stable_ui.py]] / [[daily_watchlist.py]] / profit_mining 研究基建

## 1. 目标

在「🛡️ 稳定选股」清单的 **核心层** 与 **精选层** 内部，各自把候选股按"未来更容易上涨"
细分成 **5★→1★** 的可信分级标签，帮助用户在同层内优先关注高胜率票。

**硬约束（用户拍板）：星级必须代表"真实样本外验证过的胜率差异"，不是好看的加权分。**
做法是在全历史缠论买点数据集上回测「合成分 → 胜率」关系，定出**固定分数阈值**，
再把阈值套用到每天的清单。

## 2. 现状

`daily_watchlist.py:146-153` 现已有 3 档"精选"标签（离散、非胜率分级）：

| 现档 | 命中条件 | 依据 |
|---|---|---|
| `★★核心` | 1买 + 非陷阱 + 量能金叉 | 1买内 +4.8pt 样本外 |
| `★精选` | 1买 + 非陷阱 | 基础集 +10~12pt（2024-25 OOS） |
| 空（普通） | 其余命中票 | A∪B 基础集 |

本设计在**不破坏现有 3 档层级**的前提下，给"核心"和"精选"两层各加一个层内 1-5★ 子分级。

## 3. 成功口径（用户选择：两个都算，主用 ≥4%，辅看 +10%）

复用现有前向窗口标签，零新增口径：

- **主口径（定星用）**：信号后 30 交易日内 `max(High)/进场价 - 1 ≥ 4%`
  —— 即 `build_features.py` 的 `区间涨跌幅 ≥ WIN_THRESH(=4.0)` / `是否盈利`。
- **辅口径（参考列）**：同窗 `≥ 10%` 大涨 —— `label_window.forward_window_label(win=30, thresh=0.10)`。

两者同出一个 30 日 MFE 窗口，无未来函数泄漏（`label_window.py` 已保证）。

## 4. 打分输入（信号时点特征，严防泄漏）

**数据来源校准（2026-06-02 摸查 `signal_features.csv` 97330 买点后定）**：层级（核心/精选）
在 `daily_watchlist` 里本就只由 `1买 & 非陷阱 & 量能金叉`（纯 K 线）决定，尖刺金叉只影响
"命中规则"列、不影响层级，故**层级重建 100% 可用历史数据复刻**。打分特征同样取 K 线类
（均已在 `signal_features.csv`），保证训练/线上口径一致：

| 特征 | 列名(signal_features.csv) | 取值 | 方向 |
|---|---|---|---|
| 买点类型=1买 | 买点类型 | 0/1 | 正 |
| 极限抄底（核心A 触发） | 极限抄底 | 0/1 | 正 |
| 中枢极限底 | 中枢极限底 | 0/1 | 正 |
| 中枢底部回升 | 中枢底部回升 | 0/1 | 正 |
| 量能金叉 | 量能金叉 | 0/1 | 正 |
| 量比 | 量比 | 连续 | 正 |
| 相对强弱（A 类超跌反弹越低越好） | 相对强弱 | 连续 | 负 |

**不进星级分（理由明确）**：
- `机构净买`、`获利盘%`：不在 `signal_features.csv`，且 `daily_watchlist` 线上算 `获利盘`
  依赖 baostock（可能缺失），训练/线上口径不稳。`机构净买` 研究中覆盖~80%、单信号提升度
  仅 ~1.08——按现有 `stable_ui` NOTES 的定位，作**排序加分/平手 tiebreaker**，不当星级驱动。
- `尖刺金叉`（核心B）：依赖换手率，且不参与层级判定，不入星级分。

最终权重由 §5 回测的样本外胜率增量决定；某特征若提升度不显著，回测会自然将其权重压低。

## 5. 权重标定（离线一次性回测）

1. 在**全历史缠论买点集**（26249 股 / 9379 正样本，复用 `build_features.py` →
   `signal_features.csv`）上，按主口径 ≥4% 计算每个特征的**样本外胜率增量（提升度）**。
2. 合成分 `score = Σ (权重_k · 特征_k)`，权重取该特征的提升度（正向加、惩罚项减）。
3. 走 walk-forward：训练段（≤2024）定权重与阈值，2025 段独立验证单调性。
4. 产物：`star_thresholds.json`（每层的分数切点 + 各档验证胜率 + 各档 +10% 大涨率）。

## 6. 切档（核心、精选各自独立）+ 诚实降档

- 核心群、精选群**各自**按合成分切档，要求**各档样本外胜率单调递增**（5★最高…1★最低）。
- **诚实降档（用户拍板）**：某层若回测后无法支撑 5 个单调且样本充足的档，就显示该层
  能稳定分出的档数（例如只能稳定到 3 档，则该层 3★ 封顶），并在前台注释说明原因。
  星级数永远对应真实验证过的胜率带，绝不为凑满 5 星制造假精度。
- 阈值固化进 `star_thresholds.json`，每日只做查表，不重新拟合。

## 7. 每日套用（daily_watchlist.py）

对当日每只候选算同一合成分 → 查所在层（核心/精选）阈值表 → 赋 1-N★。
CSV 新增列：

- `星级`：如 `核心★★★★☆` / `精选★★★`（层 + 实心星，封顶档明示）
- `预估胜率`：该档训练验证出的样本外 ≥4% 胜率（带"样本外/参考"字样）
- `大涨率`：该档 +10% 大涨率（辅参考）

## 8. 前台展示（stable_ui.py）

- 清单显示醒目 `星级` 列。
- 排序：**层为主键（核心 > 精选 > 普通），星数层内倒序**（契合"核心 5→1、精选 5→1"）。
- 加注：星级 = 历史样本外胜率分档，非未来保证；仍需结合大盘与基本面人工复核。

## 9. 防过拟合 / 诚实性

- walk-forward 训练/验证分段，2025 段验证单调性后才固化阈值。
- 样本不足层诚实降档（见 §6）。
- `预估胜率` 标注为样本外口径，区别于 241.9 万事件的提升度口径（后者仅锦上添花）。

## 10. 交付范围（用户选择：分两阶段）

- **阶段一（本次）**：跑离线回测，产出 `star_thresholds.json` + **回测验证报告**
  （各层各档：样本数 / ≥4% 胜率 / +10% 大涨率 / 单调性 / 实际可分档数）。
  报告存 `/home/tdxback/report/` 并加 `_YYYYMMDD_HHMMSS` 后缀（项目报告约定）。
  用户确认报告后，再进入阶段二。
- **阶段二（后续）**：改 `daily_watchlist.py` 打星 + `stable_ui.py` 展示，AppTest 验证。

## 11. 受影响文件

| 文件 | 改动 | 阶段 |
|---|---|---|
| `data/profit_mining/star_calibrate.py`（新增） | 回测打分→胜率分档，产出 json + 报告 | 一 |
| `data/profit_mining/star_thresholds.json`（新增产物） | 固化阈值 | 一 |
| `data/profit_mining/daily_watchlist.py` | 算合成分、查表打星、新增列 | 二 |
| `stable_ui.py` | 展示星级列、排序、注释 | 二 |

## 12. 验收标准

- 阶段一：报告中**每层每档胜率单调递增**（或诚实降档后单调），高星档 ≥4% 胜率显著高于
  低星档与该层基线；阈值已固化且可被 daily_watchlist 查表复现。
- 阶段二：当日清单出现 `星级/预估胜率/大涨率` 列；排序符合 §8；AppTest 通过；
  样本不足层正确降档并带注释。
